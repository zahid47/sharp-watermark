export const INVALID_IMAGE = new Error("Invalid image");
export const INVALID_RATIO = new Error("Ratio should be between 0 and 1");
export const INVALID_OPACITY = new Error("Opacity should be between 0 and 1");
export const INVALID_X_COORDINATE = new Error(
  "X coordinate should be greater than 0"
);
export const INVALID_Y_COORDINATE = new Error(
  "Y coordinate should be greater than 0"
);
export const INVALID_POSITION = new Error(
  "Invalid position. Valid positions are: center, top, bottom, left, right, topLeft, topRight, bottomLeft, bottomRight"
);
